/**
 * @file test_main.cpp
 * @brief doctest entry point.
 *
 * Kept in its own translation unit so the implementation is compiled once and
 * the actual test files stay fast to rebuild.
 */
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"
